Macro: "Check Arm Status"
Description: "Pick the color of the group track and assign it to all child tracks."
Author: "Centomila"


var trackName = getCurrentTrackName()

if (isCurrentTrackArmed()) {
    Message("${trackName} is ARMED for recording!")
}

// "!" is equal to "if not"
if (!isCurrentTrackArmed()) {
    Message("${trackName} IS NOT armed for recording!")
}